{
  "name": "actions-package-version-prefix",
  "version": "1.0.0",
  "description": "packageのバージョンに情報を付加する",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b src && ncc build -m dist/index.js && cpx src/action.yml dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5",
    "cpx": "^1.5.0",
    "typescript": "^3.7.2",
    "@types/node": "^12.12.8"
  }
}
